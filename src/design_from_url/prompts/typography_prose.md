You are documenting the typography system of a website. The screenshot shows the live page; the registry lists captured tokens.

# Registry

```yaml
<<registry>>
```

# Task

Write a brief markdown paragraph (3-5 sentences) describing typography. Cover:

- Heading family vs body family (sans-serif / serif / mono)
- Weight scale (regular / medium / bold) and visual hierarchy
- Size scale and role mapping (display, h1-h3, body, small)

Refer to tokens by their registry name when relevant (e.g. `typography.display`).

# Output

ONLY the markdown paragraph — no heading, no bullets, no surrounding code fence, no leading commentary. Output must be at least 30 characters of substantive prose.

Begin output:
