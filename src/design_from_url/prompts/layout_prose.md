You are documenting the layout system of a website. The screenshot shows the live page; the registry lists captured tokens.

# Registry

```yaml
<<registry>>
```

# Task

Write a brief markdown paragraph (3-5 sentences) describing layout primitives. Cover:

- Spacing scale (tight / comfortable / generous) and how it shapes density
- Rounded corner conventions (sharp / soft / pill) and where they appear
- How spacing and rounding support visual rhythm

Refer to tokens by their registry name when relevant (e.g. `spacing.lg`, `rounded.md`).

# Output

ONLY the markdown paragraph — no heading, no bullets, no surrounding code fence, no leading commentary. Output must be at least 30 characters of substantive prose.

Begin output:
