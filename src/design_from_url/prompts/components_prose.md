You are documenting the component conventions of a website. The screenshot shows the live page; the registry lists captured tokens including the `components` block when present.

# Registry

```yaml
<<registry>>
```

# Task

Write a brief markdown paragraph (3-5 sentences) describing component patterns. Cover:

- Primary CTA appearance (color, padding, rounding) — reference `components.button-primary` if present in the registry
- Secondary actions if visible (outline / ghost / link-styled)
- General interactive affordances (focus rings, hover hints)

If `components.button-primary` is absent, describe the inferred CTA style based on visible color tokens in the screenshot.

# Output

ONLY the markdown paragraph — no heading, no bullets, no surrounding code fence, no leading commentary. Output must be at least 30 characters of substantive prose.

Begin output:
